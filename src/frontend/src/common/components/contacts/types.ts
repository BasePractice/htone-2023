/**
 * Интерфейс контакта
 *
 * @prop {string} name Имя;
 * @prop {string} desc Описание;
 * @prop {string} email Эл почта;
 * @prop {string} phone Телефон;
 */
export interface IContact {
    name: string,
    desc: string,
    email: string,
    phone: string
}
