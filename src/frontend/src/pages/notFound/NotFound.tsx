import Typography from '@mui/material/Typography';

function NotFound() {
    return (
        <>
            <Typography variant={'h4'} fontSize={36} pt={5}>404 Страница не найдена</Typography>
        </>
    );
}

export default NotFound;
